# DESIGN-QUALITY-REPORT — مهجة كوفي مختص

## 1) المهارات المُستدعاة وكيف طُبّقت
- **ui-ux-pro-max** — شُغّل `search.py "specialty coffee cafe warm cozy" --design-system`. اعتُمد منه: لوحة قهوية دافئة (Primary `#92400E`، خلفية كريمية `#FEF3C7`)، تأثيرات (مساحات كبيرة 48px+، hover ناعم 200–300ms)، وتحذير "تجنّب الصور السيئة وإخفاء أوقات العمل" (عُولج بصور منتقاة + قسم أوقات عمل واضح). عُدّل اقتراح الخط الطفولي (Fredoka/Nunito) إلى **El Messiri + Tajawal** لأن البريف يفرضهما والمزاج "دافئ بروح" لا "مرح".
- **design-taste-frontend** — اعتُمدت "قراءة البريف" (Premium-consumer cafe). الديالات: VARIANCE 7 / MOTION 5 / DENSITY 3. تجنّبنا التِلز: لا em-dash إطلاقاً، accent واحد مقفول (العسلي)، نظام زوايا موحّد، hero يملأ الشاشة بصورة حقيقية لا gradient، لا eyebrows زائدة (≤ 1 لكل 3 أقسام)، لا أرقام مزيّفة، logo/CTA بنيّة واحدة لكل قصد. **تنبيه اللوحة:** القاعدة تحذّر من beige+brass الافتراضي للنشاطات الفاخرة؛ هنا اخترنا إسبريسو حقيقي + عسلي + كريمي وهو مبرّر دلالياً (مهجة = القلب/الروح، مقهى دافئ) لا اللجوء الكسول للبيج.
- **emil-design-eng** — منحنى easing مخصّص `cubic-bezier(.23,1,.32,1)`، أزرار `scale(.97)` عند الضغط، مدد ≤ 300ms، ease-out للدخول، scroll-reveal بـ stagger خفيف، احترام `prefers-reduced-motion`، toast يدخل من scale .97 + opacity (لا من scale 0).
- **high-end-visual-design / a11y (قواعد داخلية)** — نظام ظلال موحّد، مقياس مسافات 4/8، تباين مُتحقَّق، focus-visible 3px.

## 2) مخرجات الـ design-system المعتمدة
| العنصر | القيمة |
|---|---|
| Pattern | Hero-Centric + Conversion |
| Primary ink | `#3A2218` إسبريسو (مشتق/أعمق من اقتراح `#92400E` لتباين أعلى على الكريمي) |
| Accent | `#B97B2E` عسلي (CTA) |
| Background | `#FAF3E7` كريمي |
| Typography | El Messiri (عناوين) + Tajawal (نص) |
| Effects | hover translateY، أقسام واسعة، 200–300ms |

## 3) قرارات UI/UX أساسية
- **هوية مستقلة:** بنّي قهوي دافئ لا يشبه أي موقع سابق؛ wordmark عربي + كوب إسبريسو SVG.
- **Hero:** صورة فنجان لاتيه حقيقية + scrim متدرّج لضمان تباين النص، عنوان سطرين، CTA ظاهر فوق الطية.
- **تنوع التخطيطات:** hero / trust bar / menu grid (3) / why split / gallery masonry / order form (داكن) / location split / final CTA / footer — أكثر من 4 عائلات مختلفة، لا تكرار.
- **القائمة كاملة الشاشة:** `100vw/100dvh` خلفية إسبريسو صلبة + زر X واضح، تقفل بالـ Escape والنقر على الروابط.

## 4) سبب الألوان والخطوط
الاسم "مهجة" يعني القلب/الروح → دفء وحميمية. الإسبريسو + العسلي + الكريمي تعكس القهوة المختصة وتوفّر تبايناً ممتازاً. El Messiri خط عربي أنيق ذو طابع دافئ للعناوين، وTajawal نظيف وعالي القراءة للنص.

## 5) Accessibility
- تباين: نص `#2A1A12`/`#5A463A` على كريمي، وكريمي على إسبريسو — كلها ≥ 4.5:1.
- HTML سيمانتيك (header/nav/main/section/footer)، تدرّج h1→h3 بلا قفز.
- كل صورة لها alt عربي + width/height + lazy لغير الهيرو + decoding=async.
- aria-label على كل زر أيقونة (برغر، إغلاق، FABs، لايتبوكس)، focus-visible 3px، skip-link.
- النموذج: labels ظاهرة، أخطاء تحت الحقل، aria-live على التوست، نقل التركيز لأول حقل خاطئ.
- دعم `prefers-reduced-motion` (يوقف الحركة والكشف).

## 6) التفاعل والحركة (Emil)
ضغط الأزرار scale .97، scroll-reveal عبر IntersectionObserver مع fallback أمان (لا شيء يبقى مخفياً)، lightbox وtoast بانتقالات opacity+scale ناعمة، مدد قصيرة.

## 7) النصوص
عربية سعودية طبيعية ومحايدة جندرياً (اطلب/تواصل/تصفّح — لا احجزي/لكِ)، بدون أسعار مخترعة ("الأسعار حسب الطلب")، التقييم 4.7/1100 من قوقل فقط، بدون em-dash.

## 8) اختبار الذوق (Taste)
فاخر ودافئ ومناسب لمقهى مختص سعودي، يُقنع خلال 3 ثوانٍ بصورة الفنجان والتقييم، لا يشبه قالباً مجانياً، والمسافات/الصور/الأزرار/الحركة متناسقة. ✓

## 9) Pre-flight
لا em-dash · ثيم فاتح واحد مقفول · accent عسلي واحد · زوايا موحّدة · أزرار بتباين سليم · لا CTA يلتفّ · hero يملأ الشاشة بـ ≤ 4 عناصر نصية · صور حقيقية لا fake screenshots · لا تمرير أفقي 390px · أيقونات SVG لا emoji.
